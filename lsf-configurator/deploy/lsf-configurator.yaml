apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: configurator-role
rules:
  # Knative Serving API
  - apiGroups: ["serving.knative.dev"]
    resources: ["*"]
    verbs: ["*"]

  # Kubernetes Core API (pods, services, configmaps, secrets, etc.)
  - apiGroups: [""]
    resources: ["pods", "services"]
    verbs: ["get", "list", "watch", "create", "update", "delete"]

  # Kubernetes Apps API (deployments, statefulsets, daemonsets)
  - apiGroups: ["apps"]
    resources: ["deployments", "statefulsets", "daemonsets"]
    verbs: ["get", "list", "watch", "create", "update", "delete"]
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: configurator-sa
  namespace: configurator
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: configurator-rolebinding
subjects:
  - kind: ServiceAccount
    name: configurator-sa  
    namespace: configurator
roleRef:
  kind: ClusterRole
  name: configurator-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lsf-configurator-deployment
  namespace: configurator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lsf-configurator
  template:
    metadata:
      labels:
        app: lsf-configurator
    spec:
      serviceAccountName: configurator-sa
      terminationGracePeriodSeconds: 300
      containers:
        - name: lsf-configurator
          image: szaboegon/lsf-configurator:latest
          securityContext:
            privileged: true
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "2000m"
              memory: "1024Mi"
            limits:
              cpu: "3000m"
              memory: "3072Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 2
          env:
            - name: UPLOAD_DIR
              value: "/uploads/apps"
            - name: IMAGE_REGISTRY
              value: "registry.hub.docker.com/szaboegon"
            - name: TEMPLATES_PATH
              value: "file://templates"
            - name: BUILDER_IMAGE
              value: "docker.io/paketobuildpacks/builder-jammy-base"
            - name: REGISTRY_USER
              valueFrom:
                secretKeyRef:
                  name: registry-secret
                  key: REGISTRY_USER
            - name: REGISTRY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: registry-secret
                  key: REGISTRY_PASSWORD
            - name: REDIS_URL
              value: "redis-master.redis.svc.cluster.local:6379"
            - name: VERBOSE_LOGS
              value: "false"
            - name: METRICS_BACKEND_ADDRESS
              value: http://elasticsearch-es-http.observability.svc.cluster.local:9200
          volumeMounts:       volumeMounts:
            - name: uploadsname: uploads
              mountPath: /uploads/apps mountPath: /uploads/apps
      volumes:umes:
        - name: uploads
          persistentVolumeClaim:eClaim:
            claimName: uploads-pvc       claimName: uploads-pvc
---
apiVersion: v1 v1
kind: Service
metadata::
  name: lsf-configurator-serviceconfigurator-service
  namespace: configuratorrator
spec:
  type: NodePort  type: NodePort
  selector:  selector:
    app: lsf-configurator    app: lsf-configurator
  ports:
  - port: 80
    targetPort: 8080
    nodePort: 30001
---
apiVersion: v130001    targetPort: 8080  - port: 80  ports:  ports:
kind: PersistentVolumeClaim
metadata: 80
  name: uploads-pvc
  namespace: configurator
spec:odePort: 30001
  accessModes:
    - ReadWriteMany
  resources:tentVolumeClaim
    requests:
      storage: 10Gi
  namespace: configurator
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi


